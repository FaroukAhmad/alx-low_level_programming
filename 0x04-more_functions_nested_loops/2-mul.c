#include "main.h"
/**
 *
 * mul - functions that multiply two given integers
 *
 * @a: first integer
 * @b: second integer
 *
 */
int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
