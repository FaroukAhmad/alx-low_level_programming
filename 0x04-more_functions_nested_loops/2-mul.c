#include "main.h"
/**
 * mul - function that returns the multiplication of  two integers
 *
 * @a: one of the integers
 * @b: one of the  integers
 *
 * return: 0 success
*/
int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
	_putchar('\n');
}
